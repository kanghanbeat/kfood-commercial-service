export const metadata = {
  title: "Maps Notice"
};

export default function MapsNoticePage() {
  return (
    <main className="page-shell">
      <header className="detail-header">
        <p className="eyebrow">Maps notice</p>
        <h1>Map links are guidance, not guarantees</h1>
        <p className="detail-intro">
          Travelers should confirm hours, addresses, and closures in the linked
          map service before visiting. K-food Service tracks corrections through
          reports and verification dates.
        </p>
      </header>
    </main>
  );
}
